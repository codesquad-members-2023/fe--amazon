import { style } from '@vanilla-extract/css';

export const TwoRowTextComponentStyle = style({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'flex-start',
  alignItems: 'center',
});
